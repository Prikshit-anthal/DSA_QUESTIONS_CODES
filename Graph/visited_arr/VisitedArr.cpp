#include<iostream>
using namespace std;

int main()
{
    int n=10;
    bool visitedArr[n]={false};

    for(int i=0;i<n;i++)
    {
        if(visitedArr[i]==false)
        {
            //code here

            //maybe code may have such condn
            //it visistes other nodes
            //change them to true
        }
    }


    return 0;
}